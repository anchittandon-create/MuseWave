{
  "version": 2,
  "buildCommand": "echo 'no build needed for serverless'",
  "routes": [
    {
      "src": "/health",
      "dest": "/api/health.js"
    },
    {
      "src": "/api/generate/pipeline",
      "dest": "/api/generate/pipeline.js"
    }
  ]
}